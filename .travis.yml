sudo: false

language: go

git:
  depth: 1

install: 
  - go get github.com/gohugoio/hugo

script:
  - git submodule init
  - git submodule update
  - git checkout --orphan gh-pages
  - git reset --hard
  - git commit --allow-empty -m "Initializing gh-pages branch"
  - git checkout master
  - rm -rf public
  - mkdir public
  - git worktree prune
  - rm -rf .git/worktrees/public/s
  - git checkout master
  - rm -rf public/*
  - hugo
  - cd public/
  - cp ../CNAME .
  - git add --all
  - git commit -m "add message"
  - git push origin gh-pages:gh-pages

  # Deploy to GitHub pages
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master